# --- 核心 UI 框架 ---
streamlit>=1.30.0

# --- 数据处理 ---
# 用于将 JSON 数据转换为表格，方便展示和排序
pandas>=2.0.0

# --- 可视化 (可选) ---
# 如果后期想画出技能分布的雷达图或统计图，这两个很常用
matplotlib>=3.7.0
plotly>=5.15.0

# --- 其他工具 ---
# 确保在处理包含多语言（中文、日语）的路径或文件时更稳定
pathlib>=1.0.1

# --- 数据库连接 ---
# 用于连接 Google Sheets
st-gsheets-connection